<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Uploaded</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #222;
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .message-container {
            text-align: center;
        }
        h1 {
            color: #2ecc71;
            font-size: 36px;
            margin-bottom: 20px;
        }
        .file-url {
            color: #3498db;
            font-size: 18px;
            overflow-wrap: break-word;
        }
    </style>
</head>
<body>
<div class="message-container">
    <h1>File Uploaded Successfully</h1>
    <p>Your file has been uploaded. You can access it using the following URL:</p>
    <a class="file-url" href="{{ file_url }}" target="_blank">{{ file_url }}</a>
</div>
</body>
</html>